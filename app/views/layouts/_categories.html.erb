<div class="categories">
	<% items.each do |item| %>
		<% params = {filter: {attribute: attribute, value: item}}.to_query %>
		<div class="category">
			<%= link_to item.upcase, "/businesses?"+params, class: "button" %>
		</div>
	<%end%>
</div>
